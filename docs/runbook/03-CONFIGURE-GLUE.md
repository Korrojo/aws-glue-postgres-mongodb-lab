# 03 — Configure and Verify AWS Glue

Owner: `GLUE-030`  
Status: template until implementation

## Required completed sections

1. Explain Glue connection, crawler, Data Catalog, and job responsibilities.
2. Deploy the Glue script/package to the lab S3 prefix.
3. Inspect PostgreSQL and MongoDB connection configuration without exposing credentials.
4. Test the network/connection path using the implemented supported method.
5. Run the crawler.
6. Wait for crawler completion.
7. Verify exactly the intended `orders` and `order_items` catalog tables and schemas.
8. Explain safe crawler rerun behavior.
9. Provide focused recovery for subnet, security-group, secret, JDBC, crawler-path, and catalog-schema failures.
10. Link to `04-RUN-MIGRATION.md`.

Do not add a crawler schedule, NAT Gateway, or public database access.

